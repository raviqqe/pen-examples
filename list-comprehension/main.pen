import Core'Number
import Core'String
import System'File

main = \(ctx context) number {
  _ = File'Write(
    ctx.Os,
    File'StdOut(),
    String'Join([string Number'String(n()) for n in [number 1, 2, 3]], " "),
  )

  none
}
