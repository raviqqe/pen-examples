Map = \(xs [foo]) [bar] {
  if [x, ...xs] = xs {
    [bar convertFoo(x), ...Map(xs)]
  } else {
    [bar]
  }
}

Filter = \(xs [foo]) [foo] {
  if [x, ...xs] = xs {
    [foo ...if condition(x) { [foo x] } else { [foo] }, ...Filter(xs)]
  } else {
    [foo]
  }
}
